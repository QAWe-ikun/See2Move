diff --git a/vlnce_baselines/GPT/api.py b/vlnce_baselines/GPT/api.py
index 06ec261..4078845 100755
--- a/vlnce_baselines/GPT/api.py
+++ b/vlnce_baselines/GPT/api.py
@@ -1,6 +1,7 @@
 from openai import OpenAI
 import openai
 import base64
+import os
 from tenacity import (
     retry,
     stop_after_attempt,
@@ -13,17 +14,25 @@ from PIL import Image
 import io
 
 # openai.api_key  = "SETYOURKEY"  # GPT key
 
-generation_key = "SETYOURKEY"  # GPT key
-client = OpenAI(
-    api_key=generation_key,
-)
+client = None
+
+
+def get_client():
+    global client
+    if client is None:
+        generation_key = os.environ.get("OPENAI_API_KEY")
+        if not generation_key:
+            raise RuntimeError(
+                "OPENAI_API_KEY is required for SmartWay GPT navigation."
+            )
+        client = OpenAI(api_key=generation_key)
+    return client
 
 
 @retry(wait=wait_random_exponential(min=1, max=60), stop=stop_after_attempt(6))
 def completion_with_backoff(**kwargs):
-    return client.chat.completions.create(**kwargs)
+    return get_client().chat.completions.create(**kwargs)
 
 def encode_torch_rgb_to_base64(torch_tensor):
     if torch_tensor.is_cuda:
